{
	"name": "food-mine",
	"version": "1.0.0",
	"engines": {
		"node": ">=20 <21"
	},
	"scripts": {
		"start": "cd backend/build && node server.ts",
		"prebuild": "cd backend && npm run build",
		"build": "cd frontend && npm run build"
	}
}
